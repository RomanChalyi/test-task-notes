export const initialState = {
  notes: null,
};

const notesList = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default notesList;
